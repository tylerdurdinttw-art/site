/** Название панели и её домен — в одном месте, чтобы не расходились по экранам. */

export const APP_NAME = 'QuickPanel';

/** Домен, из которого собирается адрес проекта: quickpanel.com/<slug>. */
export const PANEL_DOMAIN = 'quickpanel.com';

/** Готовый префикс для полей со ссылкой проекта. */
export const PROJECT_URL_PREFIX = `${PANEL_DOMAIN}/`;
